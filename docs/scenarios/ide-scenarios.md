# Ferrum IDE 동작 시나리오

> **목적**: 실제 사용 상황을 시뮬레이션하여 설계 검증

---

## 시나리오 1: 첫 실행 및 프로젝트 열기

### 상황
신규 사용자가 Ferrum IDE를 처음 설치하고 기존 React/TypeScript 프로젝트를 엽니다.

### 동작 흐름

```
1. 앱 실행
   ├─ [Rust] 앱 초기화 (< 500ms)
   │   ├─ 설정 파일 로드 (~/.ferrum/config.toml)
   │   ├─ 최근 프로젝트 목록 로드
   │   └─ 테마 적용
   │
   ├─ [Frontend] 웰컴 화면 렌더링
   │   ├─ "Open Folder" 버튼
   │   ├─ 최근 프로젝트 목록
   │   └─ "New File" 버튼
   │
   └─ 사용자: "Open Folder" 클릭

2. 폴더 선택 다이얼로그
   ├─ [Rust] 네이티브 파일 다이얼로그 표시
   ├─ 사용자: ~/projects/my-react-app 선택
   └─ [Rust] 프로젝트 초기화 시작

3. 프로젝트 로딩
   ├─ [Rust] 파일 인덱싱 (병렬)
   │   ├─ ignore 크레이트로 .gitignore 적용
   │   ├─ 파일 트리 구축
   │   └─ Git 상태 로드
   │
   ├─ [Rust] LSP 서버 시작 (비동기)
   │   ├─ package.json 감지 → TypeScript
   │   ├─ typescript-language-server 프로세스 스폰
   │   ├─ Initialize 요청
   │   └─ textDocument/didOpen (열린 파일들)
   │
   ├─ [Frontend] 레이아웃 렌더링
   │   ├─ 파일 탐색기 (왼쪽 패널)
   │   ├─ 에디터 영역 (중앙)
   │   └─ 상태 바 (하단)
   │
   └─ [Frontend] "LSP 초기화 중..." 표시 → 완료 시 제거

4. 결과 화면
   ┌────────────────────────────────────────────────────────┐
   │ 📁 my-react-app                           🔧 ⚙️      │
   ├────────┬───────────────────────────────────────────────┤
   │ 📁 src │ Welcome to Ferrum                             │
   │ 📁 pub │                                               │
   │ 📄 pac │ Open a file to start editing                  │
   │ 📄 tsc │                                               │
   │ 📄 REA │                                               │
   │        │                                               │
   ├────────┴───────────────────────────────────────────────┤
   │ TypeScript ✓ │ Git: main │ Ln 1, Col 1                 │
   └────────────────────────────────────────────────────────┘
```

### 검증 포인트
- [ ] 앱 시작 시간 < 2초
- [ ] 파일 인덱싱이 UI를 블로킹하지 않음
- [ ] LSP 서버가 백그라운드에서 시작됨
- [ ] 최근 프로젝트가 영속적으로 저장됨

---

## 시나리오 2: 파일 편집 및 자동완성

### 상황
개발자가 TypeScript 파일을 열고 새로운 함수를 작성합니다.

### 동작 흐름

```
1. 파일 열기
   ├─ 사용자: 파일 탐색기에서 src/utils.ts 더블클릭
   │
   ├─ [Rust] 파일 로드
   │   ├─ fs::read_to_string(path)
   │   ├─ RopeBuffer 생성
   │   ├─ BufferId 할당
   │   └─ BufferManager에 등록
   │
   ├─ [Rust] 파싱
   │   ├─ tree-sitter TypeScript 파서 획득 (풀에서)
   │   ├─ parse(buffer.text())
   │   ├─ 하이라이팅 계산
   │   └─ SyntaxTree 캐싱
   │
   ├─ [Rust → Frontend] IPC 이벤트
   │   └─ BufferOpened { buffer_id, content, highlights }
   │
   └─ [Frontend] 에디터 탭 생성 및 렌더링

2. 코드 입력
   ├─ 사용자: "export function " 타이핑
   │
   ├─ [Frontend → Rust] IPC (각 키 입력마다)
   │   └─ EditorInsert { buffer_id, position, text }
   │
   ├─ [Rust] 버퍼 업데이트
   │   ├─ rope.insert(offset, char)
   │   ├─ Anchor 위치 조정
   │   ├─ 트랜잭션에 편집 추가
   │   └─ 증분 파싱 트리거
   │
   ├─ [Rust → Frontend] 배치 업데이트 (16ms 디바운스)
   │   └─ BufferUpdated { buffer_id, delta_highlights }
   │
   └─ [Frontend] 변경 영역만 다시 렌더링

3. 자동완성 트리거
   ├─ 사용자: "console." 입력 (. 트리거)
   │
   ├─ [Frontend → Rust] IPC
   │   └─ RequestCompletion { buffer_id, position }
   │
   ├─ [Rust] LSP 요청
   │   ├─ textDocument/completion 전송
   │   ├─ 응답 대기 (타임아웃 5초)
   │   └─ 결과 필터링/정렬
   │
   ├─ [Rust → Frontend] IPC
   │   └─ CompletionResult { items: [...] }
   │
   └─ [Frontend] 자동완성 드롭다운 표시
       ┌───────────────────┐
       │ log    function   │ ← 선택됨
       │ warn   function   │
       │ error  function   │
       │ info   function   │
       └───────────────────┘

4. 항목 선택
   ├─ 사용자: Enter 또는 Tab
   │
   ├─ [Frontend → Rust] IPC
   │   └─ ApplyCompletion { buffer_id, item }
   │
   ├─ [Rust] 완성 적용
   │   ├─ 스니펫 확장 (있는 경우)
   │   ├─ 버퍼에 텍스트 삽입
   │   └─ 커서 위치 조정
   │
   └─ [Frontend] 드롭다운 닫기, 에디터 업데이트
       console.log(|)  ← 커서가 괄호 안에
```

### 검증 포인트
- [ ] 키 입력 → 화면 반영 < 16ms (60fps)
- [ ] 자동완성 드롭다운 표시 < 100ms
- [ ] 파싱이 UI를 블로킹하지 않음
- [ ] 스니펫 탭 스톱 정상 동작

---

## 시나리오 3: Tree Viewer를 활용한 코드 탐색

### 상황
개발자가 복잡한 중첩 코드를 Tree Viewer로 빠르게 파악합니다.

### 동작 흐름

```
1. 복잡한 파일 열기
   ├─ 사용자: src/services/UserService.ts 열기 (500줄)
   │
   ├─ [Rust] 파싱 및 깊이 분석
   │   ├─ Tree-sitter 파싱
   │   ├─ DepthAnalyzer.analyze()
   │   │   └─ 각 노드의 중첩 깊이 계산
   │   ├─ FoldableNodes 추출
   │   └─ StickyHeaders 추출
   │
   └─ [Frontend] 렌더링
       ├─ 깊이별 배경색 적용
       ├─ Depth Indicator (여백 세로선)
       └─ 스티키 헤더 초기화

2. 초기 화면
   ┌─────────────────────────────────────────────────────────┐
   │ UserService.ts                                          │
   ├─────────────────────────────────────────────────────────┤
   │ ┊ class UserService {                      │ Depth 0    │
   │ ┊│  constructor(db: Database) {            │ Depth 1    │
   │ ┊│    this.db = db;                        │            │
   │ ┊│  }                                      │            │
   │ ┊│                                         │            │
   │ ┊│  async processUser(user: User) {        │ Depth 1    │
   │ ┊│├   if (user.isActive) {                 │ Depth 2    │
   │ ┊│├├    const profile = await ...          │ Depth 3    │
   │ ┊│├├    if (profile.isPremium) {           │ Depth 3    │
   │ ┊│├├├     this.sendNotification(user);     │ Depth 4    │
   └─────────────────────────────────────────────────────────┘

3. 스크롤 중 스티키 헤더
   ├─ 사용자: 아래로 스크롤 (라인 150)
   │
   ├─ [Frontend] 스크롤 이벤트
   │   └─ updateStickyHeaders(scrollTop)
   │
   └─ 스티키 헤더 업데이트
       ┌─────────────────────────────────────────────────────┐
       │ class UserService {                                 │ ← 고정
       │   async processUser(user: User) {                   │ ← 고정
       │     if (user.isActive) {                            │ ← 고정
       ├─────────────────────────────────────────────────────┤
       │ ┊│├├├     // 현재 보고 있는 코드...                  │
       │ ┊│├├├     ...                                       │
       └─────────────────────────────────────────────────────┘

4. Depth 2까지만 보기
   ├─ 사용자: Cmd+K Cmd+2 (또는 Depth Slider에서 2 선택)
   │
   ├─ [Rust] FoldState 업데이트
   │   ├─ fold_to_depth(2)
   │   ├─ Depth 3+ 노드들을 folded_ranges에 추가
   │   └─ DisplayMap 업데이트
   │
   ├─ [Rust → Frontend] IPC
   │   └─ FoldStateChanged { folded_ranges, max_depth: 2 }
   │
   └─ [Frontend] 렌더링
       ┌─────────────────────────────────────────────────────┐
       │ ┊ class UserService {                               │
       │ ┊│  constructor(db: Database) { ... }               │ ← 접힘
       │ ┊│                                                  │
       │ ┊│  async processUser(user: User) {                 │
       │ ┊│├   if (user.isActive) { ... }                    │ ← 접힘
       │ ┊│├   else { ... }                                  │ ← 접힘
       │ ┊│  }                                               │
       │ ┊│                                                  │
       │ ┊│  validateUser(user: User) {                      │
       │ ┊│├   if (!user.email) { ... }                      │ ← 접힘
       │ ┊│  }                                               │
       │ ┊}                                                  │
       └─────────────────────────────────────────────────────┘

5. 특정 블록 펼치기
   ├─ 사용자: "if (user.isActive)" 라인에서 클릭하여 펼치기
   │
   ├─ [Frontend → Rust] IPC
   │   └─ ToggleFold { buffer_id, range }
   │
   ├─ [Rust] FoldState 업데이트
   │   └─ 해당 범위만 folded_ranges에서 제거
   │
   └─ [Frontend] 해당 영역만 다시 렌더링
```

### 검증 포인트
- [ ] 깊이 분석이 파일 열기 시간에 영향을 주지 않음
- [ ] 스티키 헤더가 60fps로 업데이트됨
- [ ] Fold/Unfold가 즉각적 (< 50ms)
- [ ] 배경색이 테마와 조화로움

---

## 시나리오 4: Context Action Palette 활용

### 상황
개발자가 새 함수를 작성한 후 다음 단계를 제안받습니다.

### 동작 흐름

```
1. 함수 작성 완료
   ├─ 사용자: 새 함수 작성
   │   function calculateDiscount(price: number, rate: number) {
   │       return price * (1 - rate);
   │   }
   │
   └─ 커서가 함수 내부에 있음

2. Context Action Palette 열기
   ├─ 사용자: Cmd+Shift+P
   │
   ├─ [Rust] 컨텍스트 분석
   │   ├─ CursorContext 분석 (Tree-sitter)
   │   │   ├─ 현재 노드: function_declaration
   │   │   ├─ 함수 이름: calculateDiscount
   │   │   └─ 파라미터: price, rate
   │   │
   │   ├─ FileContext 분석
   │   │   ├─ 파일: utils.ts
   │   │   ├─ 테스트 파일 존재 여부: false
   │   │   └─ Export 여부: false
   │   │
   │   └─ ProjectContext 분석
   │       ├─ Git 상태: 변경사항 있음
   │       └─ 빌드 에러: 없음
   │
   ├─ [Rust] 액션 제안 생성
   │   ├─ Rule: FunctionWithoutTestRule → "Create Test" (90점)
   │   ├─ Rule: UnsavedChangesRule → "Save File" (100점)
   │   ├─ Rule: NoExportRule → "Add Export" (70점)
   │   ├─ Rule: NoJSDocRule → "Generate JSDoc" (60점)
   │   └─ 우선순위 정렬
   │
   └─ [Frontend] Palette 표시
       ┌────────────────────────────────────────────────┐
       │ Context Actions                            [X] │
       ├────────────────────────────────────────────────┤
       │                                                │
       │ 💾 Save File                              Tab ◀│ ← 최고 우선순위
       │    Unsaved changes                             │
       │                                                │
       │ 🧪 Create Test for 'calculateDiscount'         │
       │    Generate test file with template            │
       │                                                │
       │ 📤 Add Export                                  │
       │    Export this function                        │
       │                                                │
       │ 📝 Generate JSDoc                              │
       │    Add documentation comment                   │
       │                                                │
       └────────────────────────────────────────────────┘

3. 테스트 생성 선택
   ├─ 사용자: "Create Test" 선택 (또는 Tab 두 번)
   │
   ├─ [Rust] 테스트 생성
   │   ├─ 테스트 파일 경로 결정: utils.test.ts
   │   ├─ 테스트 템플릿 생성:
   │   │   import { calculateDiscount } from './utils';
   │   │
   │   │   describe('calculateDiscount', () => {
   │   │       it('should calculate discount correctly', () => {
   │   │           // Arrange
   │   │           const price = /* TODO */;
   │   │           const rate = /* TODO */;
   │   │
   │   │           // Act
   │   │           const result = calculateDiscount(price, rate);
   │   │
   │   │           // Assert
   │   │           expect(result).toBe(/* TODO */);
   │   │       });
   │   │   });
   │   │
   │   ├─ 파일 생성
   │   └─ 새 탭에서 열기
   │
   └─ [Frontend] 새 에디터 탭 표시
       ├─ utils.test.ts 열림
       └─ 커서가 첫 번째 TODO 위치에

4. 학습 데이터 기록
   └─ [Rust] UsageTracker.record_action()
       ├─ action_counts["CreateTest"]++
       └─ context_actions.push((context, "CreateTest"))
```

### 검증 포인트
- [ ] 컨텍스트 분석 < 50ms
- [ ] 제안이 상황에 적절함
- [ ] Tab 키로 빠르게 실행 가능
- [ ] 학습을 통해 제안이 개인화됨

---

## 시나리오 5: Visual Coding으로 데이터 흐름 시각화

### 상황
개발자가 복잡한 데이터 처리 파이프라인을 Visual Coding 뷰로 확인합니다.

### 동작 흐름

```
1. Visual Coding 뷰 열기
   ├─ 사용자: Cmd+Shift+V (또는 우측 패널에서 아이콘 클릭)
   │
   ├─ [Rust] 현재 파일 분석
   │   ├─ Tree-sitter로 AST 파싱
   │   ├─ 데이터 흐름 분석
   │   │   ├─ 변수 정의/사용 추적
   │   │   ├─ 함수 호출 체인 추출
   │   │   └─ 조건 분기 식별
   │   └─ NodeGraph 생성
   │
   └─ [Frontend] PixiJS 캔버스 초기화

2. 코드 (입력)
   const users = await fetchUsers();
   const activeUsers = users.filter(u => u.isActive);
   const premiumUsers = activeUsers.filter(u => u.isPremium);
   const emails = premiumUsers.map(u => u.email);
   await sendBulkEmail(emails, template);

3. 노드 그래프 생성 및 렌더링
   ├─ [Rust → Frontend] IPC
   │   └─ NodeGraphGenerated { nodes, connections }
   │
   └─ [Frontend] PixiJS 렌더링
       ┌────────────────────────────────────────────────────┐
       │                                                    │
       │  ┌──────────┐   ┌──────────┐   ┌──────────────┐   │
       │  │fetchUsers│──▶│  filter  │──▶│   filter     │   │
       │  │          │   │ isActive │   │  isPremium   │   │
       │  └──────────┘   └──────────┘   └──────────────┘   │
       │        │                              │            │
       │        │                              ▼            │
       │        │                       ┌──────────┐        │
       │        │                       │   map    │        │
       │        │                       │  email   │        │
       │        │                       └──────────┘        │
       │        │                              │            │
       │        │                              ▼            │
       │        │                       ┌──────────────┐    │
       │        └─────────────────────▶│sendBulkEmail │    │
       │                                │              │    │
       │                                └──────────────┘    │
       │                                                    │
       └────────────────────────────────────────────────────┘

4. 노드 호버
   ├─ 사용자: "filter isActive" 노드에 마우스 호버
   │
   ├─ [Frontend] 호버 이벤트
   │   └─ 해당 노드 하이라이트
   │
   ├─ [Frontend → Rust] IPC
   │   └─ HighlightCodeRange { range: node.code_range }
   │
   └─ [에디터] 해당 코드 라인 하이라이트
       const activeUsers = users.filter(u => u.isActive);
                                 ^^^^^^^^^^^^^^^^^^^^^^^^^ ← 하이라이트

5. 노드 드래그로 재배치
   ├─ 사용자: 노드를 드래그하여 위치 변경
   │
   ├─ [Frontend] PixiJS 드래그 핸들러
   │   └─ 노드 위치 업데이트
   │
   └─ 연결선 자동 업데이트 (베지어 곡선)

6. 노드 편집 (양방향 동기화)
   ├─ 사용자: "filter isPremium" 노드 더블클릭
   │
   ├─ [Frontend] 인라인 에디터 표시
   │   └─ 사용자: 조건을 "u.isPremium && u.verified"로 변경
   │
   ├─ [Frontend → Rust] IPC
   │   └─ UpdateNode { node_id, new_expression }
   │
   ├─ [Rust] 코드 업데이트
   │   ├─ 해당 범위의 코드 수정
   │   ├─ 버퍼 업데이트
   │   └─ 재파싱
   │
   └─ [에디터] 실시간 코드 업데이트
       const premiumUsers = activeUsers.filter(u => u.isPremium && u.verified);
```

### 검증 포인트
- [ ] 노드 그래프 생성 < 200ms
- [ ] 60fps 렌더링 (드래그, 줌, 팬)
- [ ] 코드 ↔ 비주얼 양방향 동기화
- [ ] 복잡한 코드에서도 가독성 있는 레이아웃

---

## 시나리오 6: 전역 검색 및 바꾸기

### 상황
개발자가 프로젝트 전체에서 특정 함수명을 찾아 바꿉니다.

### 동작 흐름

```
1. 전역 검색 열기
   ├─ 사용자: Cmd+Shift+F
   │
   └─ [Frontend] 검색 패널 표시 (왼쪽 사이드바)
       ┌─────────────────────────────────┐
       │ SEARCH                      [X] │
       ├─────────────────────────────────┤
       │ [________________] 🔍 Aa .*     │
       │ [________________] → Replace    │
       │                                 │
       │ Files to include: [_________]   │
       │ Files to exclude: [_________]   │
       │                                 │
       │ No results                      │
       └─────────────────────────────────┘

2. 검색 쿼리 입력
   ├─ 사용자: "getUserById" 입력
   │
   ├─ [Frontend → Rust] IPC (디바운스 200ms)
   │   └─ SearchText { query: "getUserById", options: {...} }
   │
   ├─ [Rust] ripgrep 실행 (병렬 스트리밍)
   │   ├─ ignore 패턴 적용
   │   ├─ 파일 단위로 결과 스트리밍
   │   └─ 메인 스레드 블로킹 없음
   │
   ├─ [Rust → Frontend] 스트리밍 IPC
   │   └─ SearchResult { path, line, content, matches }
   │
   └─ [Frontend] 결과 실시간 표시
       ┌─────────────────────────────────┐
       │ SEARCH                      [X] │
       ├─────────────────────────────────┤
       │ [getUserById___] 🔍 Aa .*      │
       │                                 │
       │ 23 results in 8 files           │
       │                                 │
       │ ▼ src/services/userService.ts   │
       │   3: export function getUserById│
       │   15: const user = getUserById( │
       │   42: return getUserById(id);   │
       │                                 │
       │ ▼ src/api/routes.ts             │
       │   8: import { getUserById } from│
       │   25: const user = await getUser│
       │                                 │
       │ ▼ src/tests/user.test.ts        │
       │   ...                           │
       └─────────────────────────────────┘

3. 바꾸기 입력
   ├─ 사용자: Replace 필드에 "fetchUserById" 입력
   │
   ├─ [Frontend → Rust] IPC
   │   └─ PreviewReplace { search: "getUserById", replace: "fetchUserById" }
   │
   └─ [Frontend] 미리보기 표시
       ┌─────────────────────────────────┐
       │ ...                             │
       │ ▼ src/services/userService.ts   │
       │   3: export function getUserById│
       │      →export function fetchUserB│
       │   15: const user = getUserById( │
       │      →const user = fetchUserById│
       └─────────────────────────────────┘

4. 특정 파일만 바꾸기
   ├─ 사용자: userService.ts 옆의 "Replace All in File" 클릭
   │
   ├─ [Frontend → Rust] IPC
   │   └─ ReplaceInFile { path: "src/services/userService.ts", ... }
   │
   ├─ [Rust] 파일 수정
   │   ├─ 버퍼 열기 (이미 열려있으면 재사용)
   │   ├─ 모든 매칭 바꾸기
   │   └─ 더티 플래그 설정
   │
   └─ [Frontend] 결과 업데이트
       ├─ 해당 파일 결과 제거
       └─ "20 results in 7 files" 업데이트

5. 모두 바꾸기
   ├─ 사용자: "Replace All" 버튼 클릭
   │
   ├─ [Frontend] 확인 다이얼로그
   │   └─ "Replace 20 occurrences in 7 files?"
   │
   ├─ 사용자: "Replace" 확인
   │
   ├─ [Rust] 일괄 바꾸기
   │   ├─ 각 파일별로:
   │   │   ├─ 버퍼 열기
   │   │   ├─ 바꾸기 적용
   │   │   └─ 저장 (auto-save 설정 시)
   │   └─ 진행률 보고
   │
   ├─ [Rust → Frontend] 진행 이벤트
   │   └─ ReplaceProgress { completed: 5, total: 7 }
   │
   └─ [Frontend] 완료 메시지
       └─ "Replaced 20 occurrences in 7 files"
```

### 검증 포인트
- [ ] 첫 결과 < 100ms
- [ ] 스트리밍 결과가 순차적으로 표시됨
- [ ] 미리보기가 정확함
- [ ] Undo로 일괄 바꾸기 되돌리기 가능

---

## 시나리오 7: 오류 발생 및 복구

### 상황
LSP 서버가 충돌하고 자동으로 복구됩니다.

### 동작 흐름

```
1. 정상 동작 중
   ├─ TypeScript LSP 서버 실행 중
   └─ 자동완성, 진단 정상 작동

2. LSP 서버 충돌
   ├─ [Rust] 프로세스 종료 감지
   │   └─ child.wait() → ExitStatus::Crashed
   │
   ├─ [Rust] 에러 로깅
   │   └─ error!("LSP server crashed: typescript-language-server")
   │
   ├─ [Rust → Frontend] IPC
   │   └─ LspStatus { server: "typescript", status: "crashed" }
   │
   └─ [Frontend] 상태 바 업데이트
       └─ "TypeScript ⚠️" (경고 아이콘)

3. 자동 재시작
   ├─ [Rust] 재시작 정책 확인
   │   ├─ 재시작 횟수 < 3
   │   ├─ 마지막 충돌로부터 > 30초
   │   └─ → 재시작 허용
   │
   ├─ [Rust] LSP 서버 재시작
   │   ├─ 새 프로세스 스폰
   │   ├─ Initialize 요청
   │   └─ 열린 문서 다시 등록 (didOpen)
   │
   ├─ [Rust → Frontend] IPC
   │   └─ LspStatus { server: "typescript", status: "initializing" }
   │
   └─ [Frontend] 상태 바 업데이트
       └─ "TypeScript 🔄" (로딩 아이콘)

4. 복구 완료
   ├─ [Rust] 초기화 완료
   │   ├─ 진단 재요청
   │   └─ 캐시 무효화
   │
   ├─ [Rust → Frontend] IPC
   │   └─ LspStatus { server: "typescript", status: "ready" }
   │
   └─ [Frontend] 정상 상태 복원
       └─ "TypeScript ✓"

5. 반복 충돌 시
   ├─ 재시작 횟수 >= 3
   │
   ├─ [Rust → Frontend] IPC
   │   └─ LspStatus { server: "typescript", status: "failed", message: "..." }
   │
   └─ [Frontend] 알림 표시
       ┌─────────────────────────────────────────┐
       │ ⚠️ TypeScript language server failed    │
       │                                         │
       │ The server has crashed multiple times.  │
       │ Some features may be unavailable.       │
       │                                         │
       │ [Restart Manually] [View Logs] [Ignore] │
       └─────────────────────────────────────────┘
```

### 검증 포인트
- [ ] 충돌 감지 즉시 (< 100ms)
- [ ] 자동 재시작이 UI를 블로킹하지 않음
- [ ] 재시작 후 상태 복원
- [ ] 반복 충돌 시 적절한 사용자 피드백

---

## 시나리오 8: 대용량 파일 처리

### 상황
개발자가 100,000줄 이상의 로그 파일을 엽니다.

### 동작 흐름

```
1. 대용량 파일 열기 시도
   ├─ 사용자: 파일 탐색기에서 server.log (50MB) 더블클릭
   │
   ├─ [Rust] 파일 크기 확인
   │   └─ file_size > LARGE_FILE_THRESHOLD (10MB)
   │
   └─ [Frontend] 경고 다이얼로그
       ┌─────────────────────────────────────────┐
       │ ⚠️ Large File Warning                   │
       │                                         │
       │ This file is 50MB (100,000+ lines).     │
       │ Opening it may affect performance.      │
       │                                         │
       │ Some features will be limited:          │
       │ • Syntax highlighting: Off              │
       │ • Tree Viewer: Off                      │
       │ • Minimap: Off                          │
       │                                         │
       │ [Open Anyway] [Open Read-Only] [Cancel] │
       └─────────────────────────────────────────┘

2. "Open Anyway" 선택
   ├─ [Rust] 대용량 모드로 파일 열기
   │   ├─ Rope 버퍼 생성 (청크 단위 로딩)
   │   ├─ Tree-sitter 파싱 건너뜀
   │   └─ 최소 하이라이팅만 (라인 번호)
   │
   ├─ [Frontend] 가상 스크롤링 활성화
   │   └─ 뷰포트 ± 100줄만 렌더링
   │
   └─ 파일 열기 완료 (< 3초)

3. 스크롤링
   ├─ 사용자: 빠르게 스크롤
   │
   ├─ [Frontend] 가상화 렌더링
   │   ├─ 스크롤 위치 계산
   │   ├─ 필요한 라인만 요청
   │   └─ 60fps 유지
   │
   └─ [Rust] 요청된 라인 데이터만 전송
       └─ GetLines { start: 50000, count: 200 }

4. 검색
   ├─ 사용자: Cmd+F → "ERROR" 검색
   │
   ├─ [Rust] 백그라운드 검색
   │   ├─ 별도 스레드에서 실행
   │   ├─ 진행률 보고 (10% 단위)
   │   └─ 결과 스트리밍
   │
   ├─ [Frontend] 검색 진행률 표시
   │   └─ "Searching... 45%"
   │
   └─ 결과: "1,234 matches found"
```

### 검증 포인트
- [ ] 대용량 파일 열기 < 5초
- [ ] 스크롤 60fps 유지
- [ ] 메모리 사용량 제한 (< 500MB)
- [ ] 검색이 UI를 블로킹하지 않음

---

## 검증 체크리스트 요약

### 성능
- [ ] 앱 시작: < 2초
- [ ] 파일 열기 (10KB): < 100ms
- [ ] 파일 열기 (1MB): < 500ms
- [ ] 키 입력 반응: < 16ms
- [ ] 자동완성: < 100ms
- [ ] 검색 첫 결과: < 100ms

### 안정성
- [ ] LSP 충돌 자동 복구
- [ ] 메모리 누수 없음
- [ ] 긴 작업 중 UI 반응성 유지

### 기능
- [ ] Tree Viewer 깊이 분석 정확
- [ ] Visual Coding 양방향 동기화
- [ ] Context Actions 적절한 제안
- [ ] 전역 검색/바꾸기 정확

### UX
- [ ] 오류 시 명확한 피드백
- [ ] 진행 상황 표시
- [ ] 키보드 네비게이션 완전 지원

---

## 시나리오 9: Componentify (컴포넌트 추출)

### 상황
개발자가 복잡한 JSX 코드 블록을 독립적인 React 컴포넌트로 추출합니다.

### 동작 흐름

```
1. 추출할 코드 선택
   ├─ 사용자: Dashboard.tsx에서 JSX 영역 드래그 선택
   │
   │   function Dashboard() {
   │       return (
   │           <div className="dashboard">
   │               <header>...</header>
   │               ┌─────────────────────────────────────┐
   │               │ <div className="user-card">         │ ← 선택 시작
   │               │     <img src={user.avatar} />       │
   │               │     <div className="user-info">     │
   │               │         <h3>{user.name}</h3>        │
   │               │         <p>{user.email}</p>         │
   │               │         <button onClick={onLogout}> │
   │               │             Logout                  │
   │               │         </button>                   │
   │               │     </div>                          │
   │               │ </div>                              │ ← 선택 끝
   │               └─────────────────────────────────────┘
   │               <main>...</main>
   │           </div>
   │       );
   │   }
   │
   └─ 선택된 JSX 범위 확인

2. Componentify 실행
   ├─ 사용자: Cmd+Shift+E (또는 우클릭 → "Extract to Component")
   │
   ├─ [Rust] 선택 영역 분석
   │   ├─ Tree-sitter로 JSX 구문 파싱
   │   ├─ 유효한 JSX 블록인지 확인
   │   └─ 에러 시 "Invalid selection for component extraction" 표시
   │
   ├─ [Rust] 의존성 분석
   │   ├─ 사용된 변수 추출: user, onLogout
   │   ├─ 변수 타입 추론 (LSP hover 정보 활용)
   │   │   ├─ user: User 타입
   │   │   └─ onLogout: () => void
   │   ├─ 사용된 스타일/클래스: "user-card", "user-info"
   │   └─ Import 필요 여부 분석
   │
   └─ [Frontend] 컴포넌트 생성 다이얼로그
       ┌────────────────────────────────────────────────────┐
       │ Extract to Component                           [X] │
       ├────────────────────────────────────────────────────┤
       │                                                    │
       │ Component Name:                                    │
       │ [UserCard_____________________________]            │
       │                                                    │
       │ File Location:                                     │
       │ ○ Same file (inline)                               │
       │ ● New file: [src/components/UserCard.tsx___]       │
       │ ○ Existing file: [Select...]                       │
       │                                                    │
       │ ─────────────────────────────────────────────────  │
       │ Detected Props:                                    │
       │                                                    │
       │ ☑ user: User                                       │
       │ ☑ onLogout: () => void                             │
       │                                                    │
       │ ─────────────────────────────────────────────────  │
       │ Preview:                                           │
       │ ┌──────────────────────────────────────────────┐   │
       │ │ interface UserCardProps {                    │   │
       │ │     user: User;                              │   │
       │ │     onLogout: () => void;                    │   │
       │ │ }                                            │   │
       │ │                                              │   │
       │ │ export function UserCard({                   │   │
       │ │     user,                                    │   │
       │ │     onLogout                                 │   │
       │ │ }: UserCardProps) {                          │   │
       │ │     return (                                 │   │
       │ │         <div className="user-card">          │   │
       │ │             ...                              │   │
       │ │         </div>                               │   │
       │ │     );                                       │   │
       │ │ }                                            │   │
       │ └──────────────────────────────────────────────┘   │
       │                                                    │
       │                        [Cancel]  [Extract]         │
       └────────────────────────────────────────────────────┘

3. 컴포넌트 생성 실행
   ├─ 사용자: "Extract" 클릭
   │
   ├─ [Rust] 새 파일 생성
   │   ├─ src/components/UserCard.tsx 생성
   │   ├─ 내용:
   │   │   import type { User } from '../types';
   │   │
   │   │   interface UserCardProps {
   │   │       user: User;
   │   │       onLogout: () => void;
   │   │   }
   │   │
   │   │   export function UserCard({ user, onLogout }: UserCardProps) {
   │   │       return (
   │   │           <div className="user-card">
   │   │               <img src={user.avatar} />
   │   │               <div className="user-info">
   │   │                   <h3>{user.name}</h3>
   │   │                   <p>{user.email}</p>
   │   │                   <button onClick={onLogout}>Logout</button>
   │   │               </div>
   │   │           </div>
   │   │       );
   │   │   }
   │   │
   │   └─ 파일 저장
   │
   ├─ [Rust] 원본 파일 수정
   │   ├─ Import 추가:
   │   │   import { UserCard } from './components/UserCard';
   │   │
   │   └─ 선택 영역을 컴포넌트 사용으로 대체:
   │       <UserCard user={user} onLogout={onLogout} />
   │
   └─ [Frontend] 결과 표시
       ├─ Dashboard.tsx 업데이트됨
       ├─ UserCard.tsx 새 탭으로 열림
       └─ 토스트: "Component 'UserCard' created successfully"

4. 결과 확인
   ├─ Dashboard.tsx (수정됨):
   │   import { UserCard } from './components/UserCard';
   │
   │   function Dashboard() {
   │       return (
   │           <div className="dashboard">
   │               <header>...</header>
   │               <UserCard user={user} onLogout={handleLogout} />
   │               <main>...</main>
   │           </div>
   │       );
   │   }
   │
   └─ UserCard.tsx (새로 생성됨):
       // 위 미리보기 내용과 동일
```

### 추가 기능

**1. Prop 커스터마이징:**
```
사용자가 다이얼로그에서 prop 이름 변경 가능:
- user → userData
- onLogout → handleLogout
```

**2. 스타일 추출 옵션:**
```
☐ Extract inline styles to CSS module
☐ Extract to styled-components
☐ Keep styles inline
```

**3. 테스트 파일 동시 생성:**
```
☐ Create test file (UserCard.test.tsx)
```

### 검증 포인트
- [ ] 유효하지 않은 선택 시 명확한 에러 메시지
- [ ] Props 타입 자동 추론 정확성
- [ ] Import 경로 자동 계산 정확성
- [ ] Undo로 전체 작업 되돌리기 가능 (원본 복원 + 새 파일 삭제)

---

## 시나리오 10: 템플릿 시스템 활용

### 상황
개발자가 자주 사용하는 코드 패턴을 템플릿으로 저장하고 재사용합니다.

### 동작 흐름

```
1. 템플릿에서 새 파일 생성
   ├─ 사용자: Cmd+Shift+N (New from Template)
   │
   └─ [Frontend] 템플릿 선택 다이얼로그
       ┌────────────────────────────────────────────────────┐
       │ New from Template                              [X] │
       ├────────────────────────────────────────────────────┤
       │ Search: [________________] 🔍                      │
       │                                                    │
       │ ▼ React                                            │
       │   📄 Functional Component                          │
       │   📄 Component with Props                          │
       │   📄 Component with useState                       │
       │   📄 Component with useEffect                      │
       │   📄 Custom Hook                                   │
       │   📄 Context Provider                              │
       │                                                    │
       │ ▼ TypeScript                                       │
       │   📄 Interface                                     │
       │   📄 Type with Generics                            │
       │   📄 Enum                                          │
       │                                                    │
       │ ▼ Testing                                          │
       │   📄 Jest Test Suite                               │
       │   📄 React Testing Library                         │
       │                                                    │
       │ ▼ My Templates (Custom)                            │
       │   📄 API Service                                   │
       │   📄 Redux Slice                                   │
       │                                                    │
       └────────────────────────────────────────────────────┘

2. 템플릿 선택 및 변수 입력
   ├─ 사용자: "Functional Component" 선택
   │
   └─ [Frontend] 변수 입력 다이얼로그
       ┌────────────────────────────────────────────────────┐
       │ Create: Functional Component                   [X] │
       ├────────────────────────────────────────────────────┤
       │                                                    │
       │ Template Variables:                                │
       │                                                    │
       │ Component Name: *                                  │
       │ [ProductCard________________________]              │
       │                                                    │
       │ File Name: (auto-generated)                        │
       │ [ProductCard.tsx____________________]              │
       │                                                    │
       │ Directory:                                         │
       │ [src/components/____________________] [Browse]     │
       │                                                    │
       │ ─────────────────────────────────────────────────  │
       │ Preview:                                           │
       │ ┌──────────────────────────────────────────────┐   │
       │ │ import React from 'react';                   │   │
       │ │                                              │   │
       │ │ interface ProductCardProps {                 │   │
       │ │     // TODO: Define props                    │   │
       │ │ }                                            │   │
       │ │                                              │   │
       │ │ export function ProductCard(props: Product...│   │
       │ │     return (                                 │   │
       │ │         <div>                                │   │
       │ │             {/* TODO: Implement */}          │   │
       │ │         </div>                               │   │
       │ │     );                                       │   │
       │ │ }                                            │   │
       │ └──────────────────────────────────────────────┘   │
       │                                                    │
       │                        [Cancel]  [Create]          │
       └────────────────────────────────────────────────────┘

3. 파일 생성
   ├─ 사용자: "Create" 클릭
   │
   ├─ [Rust] 템플릿 처리
   │   ├─ 템플릿 파일 로드
   │   ├─ 변수 치환: {{ComponentName}} → ProductCard
   │   ├─ 파일 생성: src/components/ProductCard.tsx
   │   └─ 관련 파일도 생성 (설정된 경우)
   │       ├─ ProductCard.module.css
   │       └─ ProductCard.test.tsx
   │
   └─ [Frontend] 새 파일 열기
       └─ 커서가 첫 TODO 위치에

4. 커스텀 템플릿 생성
   ├─ 사용자: 기존 코드 선택 → 우클릭 → "Save as Template"
   │
   └─ [Frontend] 템플릿 저장 다이얼로그
       ┌────────────────────────────────────────────────────┐
       │ Save as Template                               [X] │
       ├────────────────────────────────────────────────────┤
       │                                                    │
       │ Template Name: *                                   │
       │ [API Service_____________________________]         │
       │                                                    │
       │ Category:                                          │
       │ [My Templates____________] ▼                       │
       │                                                    │
       │ Description:                                       │
       │ [Service class for API calls___________]          │
       │                                                    │
       │ ─────────────────────────────────────────────────  │
       │ Define Variables:                                  │
       │ Click on text to mark as variable                  │
       │                                                    │
       │ ┌──────────────────────────────────────────────┐   │
       │ │ export class [UserService] {                 │   │
       │ │     private baseUrl = '/api/[users]';        │   │
       │ │                                              │   │
       │ │     async getAll(): Promise<[User][]> {      │   │
       │ │         const response = await fetch(...);   │   │
       │ │         return response.json();              │   │
       │ │     }                                        │   │
       │ │ }                                            │   │
       │ └──────────────────────────────────────────────┘   │
       │                                                    │
       │ Detected Variables:                                │
       │ • ServiceName: UserService → {{ServiceName}}       │
       │ • Endpoint: users → {{Endpoint}}                   │
       │ • EntityType: User → {{EntityType}}                │
       │                                                    │
       │                        [Cancel]  [Save]            │
       └────────────────────────────────────────────────────┘

5. 멀티파일 템플릿
   ├─ 템플릿 정의 (ferrum-templates/react-feature.json):
   │   {
   │       "name": "React Feature",
   │       "description": "Complete feature with component, hook, types",
   │       "variables": [
   │           { "name": "FeatureName", "description": "Feature name (PascalCase)" }
   │       ],
   │       "files": [
   │           {
   │               "path": "{{FeatureName}}/{{FeatureName}}.tsx",
   │               "template": "component.tsx.hbs"
   │           },
   │           {
   │               "path": "{{FeatureName}}/use{{FeatureName}}.ts",
   │               "template": "hook.ts.hbs"
   │           },
   │           {
   │               "path": "{{FeatureName}}/{{FeatureName}}.types.ts",
   │               "template": "types.ts.hbs"
   │           },
   │           {
   │               "path": "{{FeatureName}}/{{FeatureName}}.module.css",
   │               "template": "styles.css.hbs"
   │           },
   │           {
   │               "path": "{{FeatureName}}/index.ts",
   │               "template": "index.ts.hbs"
   │           }
   │       ]
   │   }
   │
   └─ 결과: 5개 파일이 한 번에 생성됨
       src/features/
       └─ ShoppingCart/
           ├─ ShoppingCart.tsx
           ├─ useShoppingCart.ts
           ├─ ShoppingCart.types.ts
           ├─ ShoppingCart.module.css
           └─ index.ts
```

### 템플릿 문법

```handlebars
{{!-- component.tsx.hbs --}}
import React from 'react';
import styles from './{{ComponentName}}.module.css';
{{#if hasProps}}
import type { {{ComponentName}}Props } from './{{ComponentName}}.types';
{{/if}}

export function {{ComponentName}}({{#if hasProps}}props: {{ComponentName}}Props{{/if}}) {
    return (
        <div className={styles.container}>
            {/* TODO: Implement {{ComponentName}} */}
        </div>
    );
}
```

### 검증 포인트
- [ ] 변수 치환 정확성
- [ ] 멀티파일 템플릿 원자적 생성 (전부 성공 또는 전부 실패)
- [ ] 커스텀 템플릿 프로젝트/전역 저장 선택
- [ ] 템플릿 공유 (export/import)

---

## 시나리오 11: 실시간 협업 (Collaboration)

### 상황
두 개발자가 같은 프로젝트에서 실시간으로 함께 코딩합니다.

### 동작 흐름

```
1. 세션 시작 (Host)
   ├─ Host: Cmd+Shift+L (또는 상태바의 "Share" 클릭)
   │
   ├─ [Rust] 협업 서버 연결
   │   ├─ WebSocket 연결 (wss://collab.ferrum.dev)
   │   ├─ 인증 (OAuth: GitHub/Google)
   │   └─ 세션 생성
   │
   └─ [Frontend] 공유 다이얼로그
       ┌────────────────────────────────────────────────────┐
       │ Start Collaboration Session                    [X] │
       ├────────────────────────────────────────────────────┤
       │                                                    │
       │ Share Link:                                        │
       │ ┌──────────────────────────────────────────────┐   │
       │ │ https://ferrum.dev/join/abc123xyz            │ 📋│
       │ └──────────────────────────────────────────────┘   │
       │                                                    │
       │ Permissions:                                       │
       │ ○ Read-only (Guests can only view)                 │
       │ ● Edit (Guests can edit files)                     │
       │ ○ Full (Guests can run terminal commands)          │
       │                                                    │
       │ Shared Scope:                                      │
       │ ○ Current file only                                │
       │ ● Entire project                                   │
       │                                                    │
       │ Session Settings:                                  │
       │ ☑ Follow my cursor (Guests auto-follow host)       │
       │ ☑ Share terminal output                            │
       │ ☐ Allow voice chat                                 │
       │                                                    │
       │                         [Copy Link]  [Start]       │
       └────────────────────────────────────────────────────┘

2. 세션 참가 (Guest)
   ├─ Guest: 링크 클릭 또는 Cmd+Shift+J → 코드 입력
   │
   ├─ [Rust] 세션 참가
   │   ├─ WebSocket 연결
   │   ├─ 프로젝트 메타데이터 수신
   │   ├─ 열린 파일 목록 수신
   │   └─ 초기 동기화 (현재 파일 내용)
   │
   └─ [Frontend] 협업 뷰 활성화
       ├─ 상태바에 참가자 표시: "👤 Alice (Host) 👤 Bob"
       └─ 원격 커서 표시 활성화

3. 실시간 편집 동기화
   ├─ Host가 코드 입력
   │
   ├─ [Rust - Host] 편집 이벤트
   │   ├─ 로컬 버퍼 업데이트
   │   ├─ Operation 생성 (CRDT 기반)
   │   │   Operation {
   │   │       site_id: "host_123",
   │   │       timestamp: 1234567890,
   │   │       type: Insert,
   │   │       position: 45,
   │   │       content: "const x = 1;"
   │   │   }
   │   └─ WebSocket으로 전송
   │
   ├─ [Server] 브로드캐스트
   │   └─ 모든 참가자에게 Operation 전송
   │
   ├─ [Rust - Guest] Operation 수신
   │   ├─ CRDT 변환 적용 (충돌 해결)
   │   ├─ 로컬 버퍼 업데이트
   │   └─ UI 업데이트 이벤트
   │
   └─ [Frontend - Guest] 실시간 반영
       └─ 텍스트 변경 + 원격 커서 이동 표시

4. 원격 커서 및 선택 표시
   ┌────────────────────────────────────────────────────────┐
   │ app.tsx                                                │
   ├────────────────────────────────────────────────────────┤
   │  1 │ import React from 'react';                        │
   │  2 │                                                   │
   │  3 │ function App() {                                  │
   │  4 │     const [count, setCount] = useState(0);        │
   │  5 │     █                      ← Host 커서 (파란색)    │
   │  6 │     ████████████████████   ← Guest 선택 (초록색)  │
   │  7 │     return (                                      │
   │  8 │         <div>                                     │
   │  9 │             <p>Count: {count}</p>                 │
   │ 10 │         </div>                                    │
   │ 11 │     );                                            │
   │ 12 │ }                                                 │
   ├────────────────────────────────────────────────────────┤
   │ 👤 Alice (Host)  │  👤 Bob (line 6)                    │
   └────────────────────────────────────────────────────────┘

5. Follow Mode
   ├─ Guest: 참가자 목록에서 Host 클릭 → "Follow"
   │
   └─ [Frontend - Guest]
       ├─ Host의 뷰포트 자동 동기화
       ├─ Host가 파일 전환 시 함께 전환
       └─ 상단에 "Following Alice" 배너 표시

6. 공동 디버깅
   ├─ Host: 브레이크포인트 설정 및 디버거 시작
   │
   ├─ [Rust] 디버그 상태 공유
   │   ├─ 브레이크포인트 위치
   │   ├─ 현재 실행 위치
   │   ├─ 변수 상태
   │   └─ 콜스택
   │
   └─ Guest도 동일한 디버그 뷰 확인 가능

7. 세션 종료
   ├─ Host: "End Session" 클릭
   │
   ├─ [Rust] 정리
   │   ├─ 모든 참가자에게 종료 알림
   │   ├─ WebSocket 연결 종료
   │   └─ 세션 데이터 정리
   │
   └─ [Frontend] 협업 모드 비활성화
       └─ 토스트: "Collaboration session ended"
```

### CRDT 충돌 해결 예시

```
초기 상태: "Hello"

Host: position 5에 " World" 삽입 → "Hello World"
Guest: position 5에 " There" 삽입 → "Hello There"

CRDT 변환 후 (양쪽 모두):
→ "Hello World There" (또는 "Hello There World" - 타임스탬프 기준)
```

### 검증 포인트
- [ ] 편집 지연 < 100ms (동일 지역)
- [ ] 충돌 해결 일관성 (모든 클라이언트 동일 결과)
- [ ] 연결 끊김 시 로컬 작업 유지
- [ ] 재연결 시 자동 동기화

---

## 시나리오 12: 설정 관리 (멀티 윈도우)

### 상황
개발자가 설정을 변경하면서 실시간으로 효과를 확인합니다.

### 동작 흐름

```
1. 설정 창 열기
   ├─ 사용자: Cmd+, (설정 단축키)
   │
   ├─ [Rust] 새 윈도우 생성
   │   └─ Tauri: create_window("settings", ...)
   │
   └─ [Frontend] 설정 전용 윈도우
       ┌────────────────────────────────────────────────────┐
       │ ⚙️  Settings                              [_][□][X]│
       ├────────────────────────────────────────────────────┤
       │ ┌──────────┐                                       │
       │ │ Search   │ 🔍 [Search settings...________]      │
       │ ├──────────┤                                       │
       │ │          │ ─────────────────────────────────────│
       │ │ General  │                                       │
       │ │ Editor   │ EDITOR                                │
       │ │ Theme    │                                       │
       │ │ Keybinds │ Font Family                           │
       │ │ Terminal │ [JetBrains Mono___________] ▼        │
       │ │ Git      │                                       │
       │ │ LSP      │ Font Size                             │
       │ │ Plugins  │ [14_] px                              │
       │ │          │                                       │
       │ │ ──────── │ Line Height                           │
       │ │ Sync     │ [1.5_]                                │
       │ │ About    │                                       │
       │ │          │ Tab Size                              │
       │ │          │ [4___] spaces                         │
       │ │          │                                       │
       │ │          │ ☑ Word Wrap                           │
       │ │          │ ☑ Show Line Numbers                   │
       │ │          │ ☐ Show Minimap                        │
       │ │          │                                       │
       │ │          │ ─────────────────────────────────────│
       │ │          │                                       │
       │ │          │ CURSOR                                │
       │ │          │                                       │
       │ │          │ Cursor Style                          │
       │ │          │ ○ Line  ● Block  ○ Underline          │
       │ │          │                                       │
       │ │          │ Cursor Blink                          │
       │ │          │ ☑ Enable                              │
       │ │          │                                       │
       │ └──────────┘                                       │
       └────────────────────────────────────────────────────┘

2. 실시간 미리보기
   ├─ 사용자: Font Size를 14 → 16으로 변경
   │
   ├─ [Rust] 설정 업데이트 (IPC)
   │   ├─ 설정 파일 업데이트 (~/.ferrum/config.toml)
   │   └─ 메인 윈도우에 이벤트 브로드캐스트
   │
   ├─ [Frontend - Main Window] 설정 변경 수신
   │   └─ 에디터 폰트 크기 즉시 변경 (16px)
   │
   └─ 사용자: 메인 윈도우에서 변경 효과 확인

3. 설정 검색
   ├─ 사용자: 검색창에 "cursor" 입력
   │
   └─ [Frontend] 필터링된 결과
       ├─ Editor > Cursor Style
       ├─ Editor > Cursor Blink
       └─ Editor > Cursor Animation

4. 설정 동기화
   ├─ 사용자: Sync 탭 선택
   │
   └─ 클라우드 동기화 옵션
       ┌─────────────────────────────────────────────┐
       │ SETTINGS SYNC                               │
       │                                             │
       │ ☑ Sync settings across devices             │
       │                                             │
       │ Account: john@example.com                   │
       │ Last synced: 2 minutes ago                  │
       │                                             │
       │ What to sync:                               │
       │ ☑ Editor settings                           │
       │ ☑ Theme                                     │
       │ ☑ Keybindings                               │
       │ ☐ Extensions                                │
       │ ☐ Snippets                                  │
       │                                             │
       │ [Sync Now]  [View Sync Log]                 │
       └─────────────────────────────────────────────┘

5. 설정 파일 직접 편집
   ├─ 사용자: "Open settings.json" 클릭
   │
   ├─ [Rust] 설정 파일을 메인 윈도우 에디터에서 열기
   │
   └─ 메인 윈도우: config.toml 탭 추가
       ├─ JSON/TOML 스키마 기반 자동완성
       └─ 저장 시 설정 자동 적용
```

### 멀티 윈도우 동기화

```
[Main Window]                    [Settings Window]
     │                                  │
     │←── Settings Changed Event ───────│
     │                                  │
     │    (font_size: 14 → 16)          │
     │                                  │
     ▼                                  │
 UI 업데이트                             │
 (에디터 폰트 크기 변경)                   │
```

### 검증 포인트
- [ ] 설정 변경 즉시 반영 (< 50ms)
- [ ] 설정 검색 퍼지 매칭
- [ ] 잘못된 설정 값 유효성 검사
- [ ] 설정 창 닫아도 메인 창 영향 없음
