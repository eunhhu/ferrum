# Ferrum IDE UI 보기 가이드

## 🚀 UI 실행 방법

### 1. 프론트엔드만 실행 (권장 - 빠른 개발)

```bash
bun vite
# 또는
npm run vite
```

- **URL**: http://localhost:1420/
- **특징**:
  - Vite의 HMR(Hot Module Replacement)로 즉시 리로드
  - Tauri 백엔드 없이 프론트엔드만 실행
  - IPC 호출은 `src/test/setup.ts`의 mock으로 처리
  - 빠른 UI 개발 및 테스트에 적합

### 2. 전체 앱 실행 (Tauri + Frontend)

```bash
bun dev
# 또는
npm run dev
```

- **특징**:
  - Rust 백엔드 + SolidJS 프론트엔드 통합 실행
  - 실제 IPC 통신 테스트 가능
  - 네이티브 윈도우로 실행
  - **주의**: 현재 Tauri 설정 오류로 실행 안 됨 (shell plugin 설정 문제)

## 📱 현재 UI 구성

### App.tsx (메인 화면)

```
┌─────────────────────────────────────────────────────┐
│ Ferrum IDE - Tree Viewer Test          (Header)    │
├──────────────────────────┬──────────────────────────┤
│                          │                          │
│   Editor Component       │   Info Panel             │
│   - Syntax highlighting  │   - Buffer ID            │
│   - Virtual scrolling    │   - Line count           │
│   - Undo/Redo           │   - Language             │
│                          │   - Features list        │
│   TreeViewer Overlay     │                          │
│   - Depth containers     │                          │
│   - Fold controls        │                          │
│                          │                          │
└──────────────────────────┴──────────────────────────┘
```

### 확인 가능한 기능

1. **Editor Component** (왼쪽)
   - TypeScript 샘플 코드 표시
   - 라인 넘버
   - 가상 스크롤링
   - 텍스트 편집 가능

2. **TreeViewer Overlay** (Editor 위에 오버레이)
   - 코드 깊이별 색상 컨테이너
   - 폴딩 가능한 라인에 접기/펼치기 버튼
   - 깊이 0~5까지 그라데이션 색상

3. **Info Panel** (오른쪽)
   - 현재 버퍼 정보
   - 구현된 기능 목록

## 🧪 테스트 실행

### 유닛 테스트

```bash
npm run test          # Watch mode
npm run test:run      # 한 번만 실행
npm run test:coverage # 커버리지 리포트
```

### 타입 체크

```bash
npm run typecheck
```

### Lint

```bash
npm run lint
npm run lint:fix  # 자동 수정
```

## 🔧 개발 팁

### 1. 컴포넌트 수정 시

- `src/components/` 아래 파일 수정
- `bun vite` 실행 중이면 자동 리로드
- 브라우저 개발자 도구로 디버깅

### 2. IPC Mock 수정

- `src/test/setup.ts`에서 mock 응답 수정
- 백엔드 없이 프론트엔드 로직 테스트 가능

### 3. 샘플 코드 변경

- `src/App.tsx`의 `SAMPLE_CODE` 상수 수정
- 다른 언어나 더 복잡한 코드로 테스트 가능

## 📂 주요 파일 위치

```
src/
├── App.tsx                          # 메인 UI (여기서 시작!)
├── components/
│   ├── editor/
│   │   └── Editor.tsx              # 에디터 컴포넌트
│   └── tree-viewer/
│       └── TreeViewer.tsx          # 트리 뷰어 컴포넌트
├── ipc/
│   ├── commands.ts                 # IPC 명령 바인딩
│   └── types.ts                    # IPC 타입 정의
├── test/
│   └── setup.ts                    # 테스트 설정 + Mock
└── __tests__/
    └── components/                 # 컴포넌트 테스트
```

## ⚠️ 현재 알려진 이슈

1. **Tauri 실행 오류**: `bun dev` 시 shell plugin 설정 오류
   - 해결: `src-tauri/tauri.conf.json`에서 shell plugin 설정 수정 필요
   - 임시 해결: `bun vite`로 프론트엔드만 실행

2. **IPC Mock 제한**: 실제 백엔드 없이는 일부 기능 제한
   - Depth 분석은 mock 데이터 사용
   - 실제 파일 열기/저장 불가

## 🎯 다음 단계

1. `bun vite` 실행
2. http://localhost:1420/ 접속
3. 에디터와 TreeViewer 동작 확인
4. 코드 수정 후 HMR로 즉시 반영 확인
