# Ferrum IDE Overview

## Vision

> **"코드를 작성하는 것이 아니라, 코드를 디자인한다."**

Ferrum은 개발자가 **디자인 도구처럼 코딩**할 수 있는 차세대 IDE입니다.  
기존 IDE들이 AI Agent에만 집중하는 사이, Ferrum은 **개발자가 직접 코드베이스를 직관적으로 탐색/수정/설계**할 수 있는 도구에 집중합니다.

**Figma가 디자인의 혁신이었듯, Ferrum은 개발의 혁신입니다.**

---

## Core Philosophy: "Code as Design"

### 핵심 철학

| Figma (디자인) | Ferrum (개발) |
|---------------|---------------|
| 캔버스에서 디자인한다 | 캔버스에서 코딩한다 |
| 드래그 앤 드롭으로 배치 | 드래그 앤 드롭으로 코드 구성 |
| Properties Panel로 설정 | Properties Panel로 Props/Styles 설정 |
| 즉각적 피드백 (0ms) | 즉각적 피드백 (**< 8ms**) |
| 디자이너도, 개발자도 사용 | 개발자도, QA도, 디자이너도 참여 |
| 코멘트로 협업 | 코드 코멘트로 협업 |

### 왜 "코드를 디자인한다"인가?

기존 개발 흐름:
```
코드 작성 → 저장 → 빌드 → 새로고침 → 확인 → 다시 코드 수정 → 반복...
```

Ferrum의 개발 흐름:
```
결과물 직접 조작 → 코드 자동 동기화 → 즉시 확인 → 완료
```

**차이점:**
- 기존: "코드"를 통해 "결과물"을 간접적으로 수정
- Ferrum: "결과물"을 직접 조작하면 "코드"가 따라옴

---

## Ferrum Design Language

### 새로운 용어 체계

Ferrum은 "코딩"이 아닌 "디자인"의 메타포를 사용합니다.

| 기존 개발 용어 | Ferrum 용어 | 설명 |
|---------------|-------------|------|
| File | **Canvas** | 작업 공간. 코드가 아닌 "디자인을 담는 캔버스" |
| Component | **Block** | 재사용 가능한 단위. 컴포넌트보다 직관적 |
| Props | **Properties** | 외부에서 주입되는 설정값 |
| State | **Local Data** | 블록 내부의 변경 가능한 데이터 |
| Event Handler | **Action** | 사용자 인터랙션에 대한 반응 |
| Import | **Link** | 다른 Canvas 연결 |
| Export | **Publish** | 다른 Canvas에서 사용 가능하게 공개 |
| Function | **Operation** | 데이터 변환/처리 단위 |

### 인터페이스 언어

**기존 IDE:**
```
"File > New > TypeScript File"
"Create Component"
"Add Import"
```

**Ferrum:**
```
"New Canvas"
"Add Block"
"Link to..."
```

---

## Instant Mode: 8ms 피드백 보장

### Figma 수준의 즉각적 반응

Ferrum의 모든 **직접 조작**은 8ms 이내에 피드백됩니다.

**Instant Mode가 적용되는 동작:**

| 동작 | 목표 시간 | Figma 비교 |
|------|----------|------------|
| 요소 선택 | **< 4ms** | ≈ 동등 |
| 선택 하이라이트 표시 | **< 4ms** | ≈ 동등 |
| 요소 드래그 (위치 변경) | **< 8ms** | ≈ 동등 |
| Properties 값 변경 | **< 8ms** | ≈ 동등 |
| Style 수정 (color, size 등) | **< 8ms** | ≈ 동등 |
| Canvas 팬/줌 | **< 8ms** | ≈ 동등 |
| 레이어 순서 변경 | **< 8ms** | ≈ 동등 |
| 호버 피드백 | **< 4ms** | ≈ 동등 |

**Async 처리되는 동작 (백그라운드):**

| 동작 | 목표 시간 | 처리 방식 |
|------|----------|----------|
| 코드 동기화 (View → Code) | < 50ms | Async, 비동기 debounce |
| LSP 정보 갱신 | < 100ms | Async, 캐시 우선 |
| 빌드/프리뷰 갱신 | < 200ms | HMR, 증분 빌드 |
| 전체 프로젝트 인덱싱 | Background | 점진적 처리 |

### 구현 원칙

```typescript
// Instant Mode 원칙
const INSTANT_MODE_RULES = {
  // 1. UI 피드백은 항상 먼저
  uiFeedbackFirst: true,
  
  // 2. 무거운 연산은 항상 비동기
  heavyOpsAsync: true,
  
  // 3. 낙관적 업데이트 (Optimistic Update)
  optimisticUpdate: true,
  
  // 4. 상태는 항상 로컬 우선
  localStateFirst: true,
};

// 예시: Properties 변경
function onPropertyChange(value: string) {
  // Step 1: UI 즉시 업데이트 (< 4ms)
  updateLocalUI(value);
  
  // Step 2: 코드 동기화 (비동기, debounced)
  debouncedSyncToCode(value);
  
  // Step 3: LSP/빌드 갱신 (백그라운드)
  queueBackgroundUpdate();
}
```

---

## Progressive Disclosure: 점진적 기능 노출

### 학습 곡선 최소화

Ferrum은 **처음 5분 안에** 기본 사용법을 익힐 수 있어야 합니다.

### 레벨별 기능 노출

#### Level 1: Familiar (0-5분)
**목표:** 기존 IDE 경험과 동일하게 시작

```
┌─────────────────────────────────────────┐
│  일반적인 코드 에디터 경험               │
│  - 파일 열기/편집/저장                   │
│  - 구문 강조, 자동완성                   │
│  - 기본 키보드 단축키                    │
│                                         │
│  💡 Hint: "Cmd+P로 Visual Mode 체험"    │
└─────────────────────────────────────────┘
```

#### Level 2: Visual Preview (5-30분)
**목표:** Visual Mode의 가치를 경험

```
┌─────────────────────────────────────────┐
│  코드 + Preview 분할 뷰                  │
│  - 실시간 프리뷰 (HMR)                   │
│  - 컴포넌트 클릭 → 코드 하이라이트        │
│  - 기본 Properties Panel                │
│                                         │
│  💡 Hint: "컴포넌트를 클릭해서 선택해보세요" │
└─────────────────────────────────────────┘
```

#### Level 3: Interactive Design (30분-2시간)
**목표:** 직접 조작의 힘을 깨달음

```
┌─────────────────────────────────────────┐
│  Production-First 경험                   │
│  - 드래그 앤 드롭으로 컴포넌트 배치        │
│  - Properties Panel에서 모든 Props 수정  │
│  - Style Inspector 사용                  │
│  - 변경사항 → 코드 자동 동기화 확인       │
│                                         │
│  💡 Hint: "Block 팔레트에서 새 요소 추가" │
└─────────────────────────────────────────┘
```

#### Level 4: Full Visual Coding (2시간+)
**목표:** "코드를 디자인한다" 완전 체험

```
┌─────────────────────────────────────────┐
│  Visual Coding 풀 활용                   │
│  - Node-based 로직 설계                  │
│  - API Designer로 백엔드 구축            │
│  - CLI Flow Designer                    │
│  - Comment System으로 협업               │
│  - 플러그인 커스터마이징                  │
└─────────────────────────────────────────┘
```

### 첫 실행 경험 (First Run Experience)

```
┌─────────────────────────────────────────────────────────────┐
│                    Welcome to Ferrum                         │
│                                                              │
│         "코드를 작성하지 말고, 디자인하세요"                  │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                                                     │    │
│  │     [5분 인터랙티브 튜토리얼 시작]                    │    │
│  │                                                     │    │
│  │     "첫 번째 Block을 디자인해보세요"                  │    │
│  │     코드 한 줄 없이 버튼 컴포넌트를 만들어봅니다       │    │
│  │                                                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  [ ] 다음부터 이 화면 표시 안 함                             │
│                                                              │
│  [튜토리얼 시작]                    [건너뛰기]                │
└─────────────────────────────────────────────────────────────┘
```

---

## Ultimate Goal: Production-First Development

### 궁극적 목표

**프로덕션 스크린(또는 그 환경)에서 즉각적으로 개발하고 디자인한다.**

> "코드를 작성하는 것이 아니라, 제품을 직접 만지며 개발한다."

### Production-First 시나리오

#### 1. Web Frontend (React, Vue, Svelte 등)

**Figma/Framer 스타일 경험:**
```
┌─────────────────────────────────────────────────────────┐
│  [Toolbar]  Select | Hand | Text | Box | Block | ...   │
├──────────┬──────────────────────────────────┬───────────┤
│ Layers   │                                  │ Properties│
│ ──────── │                                  │ ───────── │
│ ▼ Page   │  ┌─────────────────────────┐     │ Block:    │
│   ▼ Hero │  │   [Header]   ← 클릭 선택 │     │  Header   │
│     Logo │  │   ─────────────────────  │     │           │
│     Nav  │  │                         │     │ Properties│
│   ▼ Main │  │   [Content]             │     │  title    │
│     Card │  │   실제 렌더링된 UI       │     │  [입력]   │
│     Card │  │                         │     │           │
│   Footer │  │   [Button] ← 드래그 이동 │     │ Styles    │
│          │  └─────────────────────────┘     │  padding  │
│          │                                  │  color    │
└──────────┴──────────────────────────────────┴───────────┘
```

**핵심 기능:**
- 렌더링된 Block 직접 클릭/선택
- Properties Panel에서 Props/Styles 실시간 수정
- 드래그로 Block 순서 변경
- 변경사항 → JSX/TSX 코드 자동 동기화 (< 50ms)

#### 2. Backend API (Express, Fastify, Elysia 등)

**Postman/Apidog 스타일 경험:**
```
┌─────────────────────────────────────────────────────────┐
│  API Designer - users.ts                                │
├─────────────────────────────────────────────────────────┤
│  Endpoints                                              │
│  ─────────────────────────────────────────────────────  │
│  │ GET    │ /api/users          │ List users   │ [▶] │  │
│  │ POST   │ /api/users          │ Create user  │ [▶] │  │
│  │ GET    │ /api/users/:id      │ Get by ID    │ [▶] │  │
│  └────────────────────────────────────────────────────  │
│                                                         │
│  [+ Add Endpoint]                                       │
├─────────────────────────────────────────────────────────┤
│  Request Builder                                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ POST /api/users                      [Send ▶]   │   │
│  │ Headers:  Authorization: Bearer [token]          │   │
│  │ Body:     { "name": "John", "email": "..." }    │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  Response  │ Status: 201  │ Time: 45ms                  │
│  { "id": "123", "name": "John", ... }                  │
└─────────────────────────────────────────────────────────┘
```

#### 3. CLI Application

**Flow Designer 스타일 경험:**
```
┌─────────────────────────────────────────────────────────┐
│  CLI Flow Designer                                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌─────────┐    ┌───────────────┐    ┌───────────┐    │
│   │  Start  │───→│   Prompt:     │───→│ Condition │    │
│   │         │    │ "Enter name?" │    │ if (name) │    │
│   └─────────┘    └───────────────┘    └─────┬─────┘    │
│                                         ┌───┴───┐      │
│                                     Yes │       │ No   │
│                                         ▼       ▼      │
│                                   ┌───────┐ ┌───────┐  │
│                                   │ Greet │ │ Exit  │  │
│                                   └───────┘ └───────┘  │
├─────────────────────────────────────────────────────────┤
│  Terminal Preview                                       │
│  ┌─────────────────────────────────────────────────┐   │
│  │ $ my-cli                                         │   │
│  │ ? Enter name: █                                  │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### 4. Mobile App (React Native, Flutter)

**Device Emulator 스타일 경험:**
```
┌─────────────────────────────────────────────────────────┐
│  Mobile Designer                                        │
├──────────┬──────────────────────────────────┬───────────┤
│ Layers   │      ┌───────────────┐           │ Properties│
│ ──────── │      │ ┌───────────┐ │           │ ───────── │
│ ▼ App    │      │ │  Header   │ │ ← iPhone  │ Block:    │
│   Screen1│      │ ├───────────┤ │   Preview │  Button   │
│   Screen2│      │ │           │ │           │           │
│          │      │ │  Content  │ │           │ variant   │
│          │      │ │           │ │           │ [primary] │
│          │      │ ├───────────┤ │           │           │
│          │      │ │ [Button]  │◀── 선택됨   │ onPress   │
│          │      │ └───────────┘ │           │ [Action]  │
│          │      └───────────────┘           │           │
│          │      iPhone 14 Pro               │           │
│          │      [Device ▼] [Rotate]         │           │
└──────────┴──────────────────────────────────┴───────────┘
```

#### 5. Database Schema (Prisma, SQL)

**Schema Designer 스타일 경험:**
```
┌─────────────────────────────────────────────────────────┐
│  Database Designer - schema.prisma                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌──────────────┐         ┌──────────────┐            │
│   │     User     │         │     Post     │            │
│   ├──────────────┤         ├──────────────┤            │
│   │ id       Int │────┐    │ id       Int │            │
│   │ email String │    │    │ title String │            │
│   │ name  String │    └───→│ authorId Int │            │
│   │ posts Post[] │         │ content Text │            │
│   └──────────────┘         └──────────────┘            │
│                                                         │
│   [+ Add Model]  [+ Add Relation]  [Generate Migration] │
└─────────────────────────────────────────────────────────┘
```

#### 6. 3D/Game (Three.js, PixiJS)

**Scene Designer 스타일 경험:**
```
┌─────────────────────────────────────────────────────────┐
│  Scene Designer                                         │
├──────────┬──────────────────────────────────┬───────────┤
│ Scene    │                                  │ Properties│
│ ──────── │      ┌─────────────────────┐     │ ───────── │
│ ▼ Scene  │      │        ▲            │     │ Mesh:     │
│   Camera │      │       /│\           │     │  Cube     │
│   Light  │      │      / │ \          │     │           │
│   ▼ Group│      │     /  │  \   ← 3D  │     │ Position  │
│     Cube │      │    ────┼────   View │     │  x: 0     │
│     Sphere      │        │            │     │  y: 1     │
│          │      │        │            │     │  z: 0     │
│          │      └─────────────────────┘     │           │
│          │      [Orbit] [Pan] [Zoom]        │ Material  │
│          │                                  │  [Select] │
└──────────┴──────────────────────────────────┴───────────┘
```

### Context-Aware View Modes

**핵심 개념: 파일의 export, mimetype, 패턴에 따라 자동으로 최적의 View Mode 제안**

| 조건 | 제안되는 View Mode |
|------|-------------------|
| `*.tsx` + React Component export | **Web UI Editing** |
| `*.ts` + Elysia/Express export | **API Designer** |
| `*.ts` + Commander export | **CLI Flow Designer** |
| `*.tsx` + React Native Component | **Mobile Designer** |
| `*.prisma` | **Database Designer** |
| `*.tsx` + Three.js Scene | **Scene Designer** |
| `*.graphql` | **GraphQL Designer** |
| `*.json` + `package.json` | **Package Manager UI** |
| `*.md` | **Markdown Preview** |

---

## Code Design System

### Design Tokens (코드 버전)

**Ferrum은 프로젝트의 Design System을 시각적으로 편집할 수 있습니다.**

```
┌─────────────────────────────────────────────────────────┐
│  Design System - tailwind.config.js                     │
├─────────────────────────────────────────────────────────┤
│  Colors                                                 │
│  ─────────────────────────────────────────────────────  │
│  primary     [████████] #3B82F6  ← 클릭하여 편집        │
│  secondary   [████████] #6366F1                         │
│  success     [████████] #22C55E                         │
│  danger      [████████] #EF4444                         │
│                                                         │
│  Typography                                             │
│  ─────────────────────────────────────────────────────  │
│  Font Family   [Inter, system-ui, sans-serif]           │
│  Base Size     [16px ▼]                                 │
│  Scale Ratio   [1.25 ▼] (Major Third)                   │
│                                                         │
│  Spacing                                                │
│  ─────────────────────────────────────────────────────  │
│  Base Unit     [4px]                                    │
│  Scale         [1, 2, 4, 6, 8, 12, 16, 24, 32...]      │
│                                                         │
│  [Preview Changes]           [Apply to Config]          │
└─────────────────────────────────────────────────────────┘
```

### Block Library (Component Gallery)

**프로젝트 내 모든 Block을 갤러리로 탐색:**

```
┌─────────────────────────────────────────────────────────┐
│  Block Library                              [Search 🔍] │
├─────────────────────────────────────────────────────────┤
│  📁 Primitives                                          │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                       │
│  │ Btn │ │Input│ │ Txt │ │ Img │  ← 드래그 앤 드롭     │
│  └─────┘ └─────┘ └─────┘ └─────┘                       │
│                                                         │
│  📁 Layout                                              │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                       │
│  │Stack│ │ Flex│ │ Grid│ │ Card│                       │
│  └─────┘ └─────┘ └─────┘ └─────┘                       │
│                                                         │
│  📁 Features                                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                │
│  │  Header  │ │  Footer  │ │  Sidebar │                │
│  └──────────┘ └──────────┘ └──────────┘                │
│                                                         │
│  [+ Create Block]                [Import from Figma]   │
└─────────────────────────────────────────────────────────┘
```

---

## Philosophy & Priorities

### 우선순위
```
DX (개발 편의성) > 즉각적 피드백 > 생산성 > 성능 > 확장성 > 디자인 > 기타
```

### 설계 원칙

| 원칙 | 설명 |
|------|------|
| **Instant Feedback** | 모든 직접 조작은 8ms 이내 피드백 (Figma 수준) |
| **Visual First** | 가능한 시각적으로 표현, 필요시 코드로 전환 |
| **Progressive Disclosure** | 복잡한 기능은 점진적으로 노출 |
| **Two-Way Sync** | View ↔ Code 양방향 완벽 동기화 |
| **Non-Destructive** | 언제든 Undo/Redo, 변경 이력 보존 |
| **Full Control** | 에디터 자체 구현으로 완전한 커스터마이징 |
| **VSCode Parity+** | VSCode 편의 기능 전부 포함 + 혁신 추가 |

---

## Target Users

### Primary

- **모든 프론트엔드 개발자** - React, Vue, Svelte 등
- **풀스택 개발자** - 프론트엔드 + 백엔드 통합 경험
- **디자인 시스템 관리자** - Design Tokens 시각적 편집

### Secondary

- **백엔드 개발자** - API Designer, Database Designer
- **QA 엔지니어** - Comment System으로 코드 레벨 피드백
- **디자이너** - 코드 이해 없이 Properties 수정 참여

### User Journey

```
초보 개발자                              시니어 개발자
    │                                        │
    ▼                                        ▼
Level 1: 익숙한 에디터 경험    ───────→   Level 4부터 시작
    │                                        │
    ▼                                        │
Level 2: Visual Preview 발견               │
    │                                        │
    ▼                                        │
Level 3: 직접 조작의 힘        ◀───────────┘
    │
    ▼
Level 4: 완전한 Visual Coding
```

---

## Business Model

- **유료 제품** (오픈소스 아님)
- 판매 방식: TBD (라이센스? 구독? Freemium?)

---

## Platform Support

### Desktop (Primary)
- macOS, Windows, Linux
- 로컬 개발 환경
- 풀 기능 지원

### Mobile (Secondary)
- iOS, Android
- **클라우드 기반 개발만** (로컬 개발 미지원)
- **텍스트 코딩 미지원** → Visual Coding 전용
- 별도 UI 설계
- 모바일 전용 제스처 지원

---

## Architecture

### Desktop

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│  ┌─────────────────┐  ┌─────────────────────────────────┐   │
│  │    SolidJS      │  │           PixiJS                │   │
│  │  (Pure View)    │  │  (WebGPU/WebGL2 Visualization)  │   │
│  │                 │  │  - Production Preview           │   │
│  │  - DOM Text     │  │  - Visual Coding (Node-based)   │   │
│  │  - UI Components│  │  - Canvas Interactions          │   │
│  └────────┬────────┘  └────────────────┬────────────────┘   │
│           │                            │                     │
│           └────────────┬───────────────┘                     │
│                        │ IPC (Tauri)                         │
├────────────────────────┼────────────────────────────────────┤
│  ┌─────────────────────▼─────────────────────────────────┐  │
│  │                   Rust Backend                         │  │
│  │                                                        │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │  │
│  │  │ Text Buffer  │  │  AST Engine  │  │  LSP Client │  │  │
│  │  │   (ropey)    │  │ (tree-sitter)│  │  (Official) │  │  │
│  │  └──────────────┘  └──────────────┘  └─────────────┘  │  │
│  │                                                        │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │  │
│  │  │ View Sync    │  │    Search    │  │    State    │  │  │
│  │  │ (Two-Way)    │  │    (grep)    │  │  Management │  │  │
│  │  └──────────────┘  └──────────────┘  └─────────────┘  │  │
│  └────────────────────────────────────────────────────────┘  │
│                        Backend                               │
└─────────────────────────────────────────────────────────────┘
```

### 역할 분리

| Layer | 역할 |
|-------|------|
| **SolidJS** | 순수 View 렌더링만 (상태/연산 없음) |
| **PixiJS** | 고성능 Canvas (프리뷰, 비주얼 코딩, 상호작용) |
| **Rust** | 상태관리, 연산, 파싱, 인덱싱, View↔Code 동기화 |

---

## Tech Stack

| Category | Choice | Reason |
|----------|--------|--------|
| **Framework** | Tauri 2.0 | Native 성능, 작은 번들 |
| **Frontend** | SolidJS | No VDOM, 최고 성능 |
| **Styling** | Tailwind CSS 4.x | 빠른 개발 |
| **Visualization** | PixiJS | WebGPU/WebGL2, Canvas 고성능 |
| **Text Buffer** | ropey | Rope 자료구조, 대용량 파일 |
| **AST Parsing** | tree-sitter | 범용 언어, 증분 파싱 |
| **LSP** | 공식 LSP 서버 | 오프라인시 번들 LSP fallback |
| **IPC** | Tauri + MsgPack | 고속 직렬화, Delta 업데이트 |
| **Plugin Runtime** | WASM (Wasmtime) | 고성능, 보안, 언어 중립 |

---

## Core Features

### Design Canvas (Editor)

| Feature | Description |
|---------|-------------|
| Instant Selection | 4ms 이내 선택 피드백 |
| Drag & Drop | 8ms 이내 드래그 피드백 |
| Properties Panel | 실시간 Props/Styles 편집 |
| Layers Panel | Figma 스타일 레이어 관리 |
| Smart Selection Expansion | AST 기반 구문 단위 선택 확장 |
| Block Commenting | 블록 단위 코멘트 |

### Navigation & Exploration

| Feature | Description |
|---------|-------------|
| **Tree Viewer** | 깊이별 컬러 + 스티키 헤더 (Figma Layers 스타일) |
| **Tree Fold** | 피그마식 레이어 접근 + n-depth 이하 fold |
| **Navigation Trail** | "어떻게 여기까지 왔는지" 히스토리 |
| **Dependency Highlight** | 의존 파일에 라인 연결 / BG 하이라이트 |
| **Related Files** | 연관 파일 그룹핑 (Component + Test + Types) |

### Visual Features (Canvas)

| Feature | Description |
|---------|-------------|
| **Production Preview** | 실제 렌더링된 UI에서 직접 조작 |
| **Visual Coding** | Node-based 로직 설계 |
| **Two-Way Sync** | View ↔ Code 양방향 동기화 |
| **Structural Minimap** | 구조 기반 미니맵 |

### Collaboration

| Feature | Description |
|---------|-------------|
| **Comment System** | Figma 스타일 코멘트, AST 기반 위치 추적 |
| **Terminal Integration** | 통합 터미널 |
| **Vim Mode** | Vim 키바인딩 에뮬레이션 |
| **Template System** | Block 템플릿, 멀티파일 지원 |

### Production-First View Modes

| Feature | Description |
|---------|-------------|
| **Web UI Editing** | Figma/Framer 스타일 |
| **API Designer** | Postman/Apidog 스타일 |
| **CLI Flow Designer** | Node-based CLI 시각화 |
| **Mobile Designer** | React Native/Flutter 프리뷰 |
| **Database Designer** | Prisma/SQL 스키마 시각화 |
| **Scene Designer** | Three.js/PixiJS 3D/2D 편집 |

---

## Performance Targets

### Instant Mode (직접 조작)

| 동작 | 목표 시간 | 중요도 |
|------|----------|--------|
| 요소 선택 | **< 4ms** | Critical |
| 드래그 피드백 | **< 8ms** | Critical |
| Properties 변경 | **< 8ms** | Critical |
| 호버 피드백 | **< 4ms** | Critical |
| Canvas 팬/줌 | **< 8ms** | Critical |

### Standard Performance

| 작업 | 목표 시간 | 비고 |
|------|----------|------|
| 키 입력 → 화면 반영 | < 16ms | 60fps 유지 |
| View ↔ Code 동기화 | < 50ms | Debounced |
| 파일 열기 (10KB) | < 100ms | |
| 파일 열기 (1MB) | < 500ms | |
| 자동완성 표시 | < 100ms | 첫 결과 |
| 정의로 이동 | < 200ms | 캐시시 < 50ms |
| 앱 시작 | < 2s | 콜드 스타트 |

---

## Roadmap

### MVP (현재 목표)

#### Foundation
- [ ] 자체 에디터 엔진 (ropey + tree-sitter)
- [ ] Instant Mode 구현 (< 8ms 피드백)
- [ ] 기본 Canvas 시스템
- [ ] LSP 연동
- [ ] 번들 LSP (16개 언어)

#### Core Design Experience
- [ ] Properties Panel
- [ ] Layers Panel (Tree Viewer)
- [ ] Block Library
- [ ] Two-Way Sync (View ↔ Code)
- [ ] Smart Selection Expansion

#### Visual Modes
- [ ] Web UI Editing
- [ ] API Designer
- [ ] CLI Flow Designer
- [ ] Production Preview (HMR)

#### Collaboration
- [ ] Comment System (Figma-style)
- [ ] Terminal Integration
- [ ] Vim Mode

### Post-MVP

#### Expanded View Modes
- [ ] Mobile Designer (React Native, Flutter)
- [ ] Database Designer (Prisma, SQL)
- [ ] Scene Designer (Three.js, PixiJS)

#### Ecosystem
- [ ] Plugin System (WASM)
- [ ] Plugin Marketplace
- [ ] Theme System
- [ ] Design System Editor

#### Advanced Collaboration
- [ ] 실시간 협업 (CRDT)
- [ ] 원격 커서 동기화

---

## Related Documents

### 아키텍처
- [architecture.md](./architecture.md) - 전체 아키텍처 및 데이터 타입
- [architecture/editor-engine.md](./architecture/editor-engine.md) - Rope, Anchor, DisplayMap
- [architecture/ipc-protocol.md](./architecture/ipc-protocol.md) - Tauri IPC 최적화
- [architecture/view-mode-system.md](./architecture/view-mode-system.md) - 뷰 모드 시스템

### 기능 스펙
- [specs/visual-coding.md](./specs/visual-coding.md) - Visual Coding
- [specs/comment-system.md](./specs/comment-system.md) - Figma-style Comment System
- [specs/additional-features.md](./specs/additional-features.md) - 20개 추가 기능

### 기술 문서
- [technical/plugin-system.md](./technical/plugin-system.md) - 플러그인 시스템
- [technical/frontend-optimization.md](./technical/frontend-optimization.md) - 프론트엔드 최적화
- [specs/design-language.md](./specs/design-language.md) - Ferrum Design Language
- [specs/progressive-disclosure.md](./specs/progressive-disclosure.md) - Progressive Disclosure UX

### 리서치 & 검토
- [research/zed-analysis.md](./research/zed-analysis.md) - Zed 분석
- [review/document-review.md](./review/document-review.md) - 문서 검토 리포트

---

## Open Questions

1. **Visual Coding 스펙** - 언어/프레임워크별 구체 설계
2. **JS Plugin 런타임** - V8 vs QuickJS?
3. **클라우드 백엔드 (모바일)** - 자체 구축? 기존 서비스 연동?
4. **로컬 AI 모델** - 어떤 오픈소스 모델?
5. **판매 모델** - 라이센스? 구독? Freemium?
